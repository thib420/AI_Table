// Shared exports

// Contexts
export { AuthProvider, useAuth } from './contexts/AuthContext';

// Lib utilities
export { createClient } from './lib/supabase/client';
export { cn } from './lib/utils';

// Types (when we add them)
// export * from './types'; 